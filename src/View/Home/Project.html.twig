{% extends 'layout.html.twig' %}

 {% block title %}
     Projet
 {% endblock %}

{% block content %}
    <h3>{{ project.title }}</h3>

        <div id="carouselExampleFade" class="carousel slide carousel-fade" data-ride="carousel">
        <div class="carousel-inner">
            {% for picture in pictures %}
                <div {% if picture.is_main == '1' %} class="carousel-item active" {% else %} class="carousel-item" {% endif %}>

                <img src="/assets/images/{{ picture.name }}" class="d-block w-100" alt="{{ picture.name }}">
            </div>
            {% endfor %}

            {# <div class="carousel-item">
                <img src="/assets/images/NewDeal_Institut_2.jpg" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="/assets/images/NewDeal_Institut_3.jpg" class="d-block w-100" alt="...">
            </div> #}
        </div>

        </div>
    <a class="carousel-control-prev" href="#carouselExampleFade" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleFade" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
    {{ project.description }}
    {{ project.promo }}
    {{ project.name }}

{% endblock %}